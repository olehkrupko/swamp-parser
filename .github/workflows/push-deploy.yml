name: push-deploy
run-name: Deploy update from ${{ github.ref_name }}
on:
  push:
    branches:
      - 'main'
      - '10-fastapi-routes'
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          path: ${{github.job}}
